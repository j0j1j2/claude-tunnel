{
  "name": "claude-tunnel",
  "version": "0.1.0",
  "type": "module",
  "private": true,
  "bin": {
    "claude-tunnel": "./bin/claude-tunnel.ts"
  },
  "scripts": {
    "install:tunnel": "bun run scripts/install.ts",
    "uninstall:tunnel": "bun run scripts/uninstall.ts",
    "mcp": "bun run src/mcp-server.ts",
    "broker": "bun run src/broker.ts",
    "cli": "bun run bin/claude-tunnel.ts",
    "test:smoke": "bun run test/smoke.ts",
    "test:mcp": "bun run test/mcp-handshake.ts",
    "test:hook": "bun run test/hook.ts"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.29.0"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  }
}
